import 'package:flutter/material.dart';

class AppStyling {
  /// Heights
  static const inputHeight = 65.0;

  /// Widths
  static const borderWidthS = 2.0;

  /// Radiants
  static const borderRadiusM = BorderRadius.all(Radius.circular(16));

  /// TextStyle
  static const textFieldTextStyle = TextStyle(
    fontWeight: FontWeight.w600,
    fontSize: 20,
    fontFamily: 'Nunito',
  );
}
